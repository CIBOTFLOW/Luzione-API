# OPS-CONTRACTS-CORRECTION-03 working contract

- Project/outcome: close only D01-D06 and B07-B11 from `OPS-CONTRACTS-ASSURE-03` by making the v3 operations-evidence boundary source-authenticated and cross-invocation append-only while every decision credit remains zero.
- Entrypoint/end state: an offline API-owned verifier supplies a strict `LuzioneOperationsEvidenceLedger/v3`, an exact verifier clock, authenticated canonical source attestations and an append-state store. The SDK either atomically appends a new state, returns an exact idempotent replay, or raises a typed fail-closed compatibility error.
- Truth/write owner/readback: Luzione API owns the contracts, strict parser and append-state protocol. Exact source object/readback bytes must verify against the pinned synthetic-only trust root and their typed membership, approval, incident or recovery meaning. A future real source adapter is absent. L2/L3 remain exact `ABSENT`/disconnected and may not mint authority.
- Consumes/publishes: consumes frozen `LuzioneOperationsEvidenceLedger/v2`, controller authority `b20899aa38b3e57aa809924266d9f68a94495468`, assurance canonical JSON SHA-256 `02c7b353f9fbc43cd78f0af096c55a9622a68158794f1735924a29aa036af4a8` and raw packet SHA-256 `e33687257cf0b8e1c8d8061b793c456e11d7e28c16e04f41b8fb40b83131ffe8`; publishes the repaired v3 strict boundary plus `OperationsEvidenceAppendState/v1`, `OperationsEvidenceCanonicalSourceAttestation/v1` and their schema/SDK/fixtures/consumer packets.
- Dependencies/mutation cone: exact base `1607fd10720922a0ce04da1e79db3c60dc8835b3`; operations-evidence v3 schema/manifest, SDK/types/rules/fixtures/tests, package/test metadata and execution evidence only.
- Reuse/freeze: v3 continues to invoke frozen v2 structural validation. Operations v1/v2, Core, onboarding (including Correction-02), effect-admission and connector-revocation trees remain byte-identical.
- Invariants: stable grant identity survives parser invocations; exact replay is idempotent; changed approval/signer/action/stage/effect/expiry/state/digest conflicts; source hashes are recomputed over exact canonical bytes and signatures verify against a pinned non-production key; recovery is incident/capability typed; append state is content-addressed and single-successor; stale state, gaps, forks, cycles, duplicate successors and reused epochs fail closed; capability credit is strictly after the latest verified reset; v1/v2 decision use remains prohibited; proof-day/G2/production credit remains zero.
- Non-scope: real source authority or durable adapter, routes, relations, migrations, production/database queries, credentials, providers, hosted effects, proof-window credit, G2 grants, deployment/promotion, merge/default action, destructive action or rollback.
- Acceptance proof: digest-verify the authoritative assurance packet; pass independent D01-D06 and B07-B11 fixtures; exact replay/change-conflict and two-ledger fork tests; source-signature/type/schema parity; reset-day denial with calendar/30-day preservation; focused/full compliance/typecheck/lint/test/build; frozen-tree and reverse-apply proof; exact-head CI and Git-bound preview truth if automatically available; detached exact-final attestation without self-hash placeholders.
- Irreversible effects: none. Reversing this additive documentation/schema/SDK/test cell restores the exact base tree; no production rollback authority is created.
