# CONNECTOR-REVOCATION-L1-CORRECTION-01 working contract

- Outcome: replace the route-reachable caller-owned revocation tuple with an additive v2 request/receipt boundary whose current connector and opaque credential metadata are resolved by the API server before P110 reservation.
- Entrypoint/end state: a service-authenticated `POST /api/v1/connectors/revocations` proposes exact-match selectors; the server either fails closed or appends a `ConnectorRevocationReceipt/v2` whose digest binds the canonical owner resolution and P110 outcome. Same-tenant `GET` remains authoritative readback.
- Truth/write owner/readback: a `CanonicalConnectorBindingResolver` is the only input for current `ConnectorBinding/v1`, provider-account, destination and `ConnectorCredentialHandle/v2` truth. The default resolver is unavailable and denies; no caller or emulator can mint that truth. P110 and the append-only receipt table remain finality/readback owners.
- Consumes/publishes: consumes frozen `ConnectorBinding/v1`, corrected effect-admission v2/P110 execution lineage and connector revocation v1 evidence; publishes `ConnectorRevocationRequest/v2`, `ConnectorRevocationReceipt/v2`, `ConnectorCredentialHandle/v2`, `CanonicalConnectorBindingResolution/v1`, strict schemas, server resolver, emulator-only service and consumer packet.
- Dependencies/base: exact API final `0c970c0b54f190ba0b2d97629311d2712f9518e2`; controller `8dbcc9b9b7429d00831aeb16139bd06644ea36bf`; L5 assurance fingerprint `b2e1923f459266b87ba54d77e189655e226eaacf566447a0107ecde1dbde51f3`.
- Mutation cone: additive v2 contracts/schemas/parsers, revocation route wiring, isolated reversible receipt-column migration, disposable proof, tests and execution metadata. No credential lookup, erasure, live adapter, provider call, production query/migration, deployment or activation.
- Invariants: strict raw values are never trimmed or timestamp-normalized; exact field/version/digest parity; selector values are only optimistic guards; current same-tenant BOUND resolution is server-derived; stale/fabricated/drifted truth fails before reservation; containment and normal kill state are freshly re-read before future credential hold and immediately before execute or disposition; credentials remain opaque and retained; emulator is the sole adapter; append-only evidence and P110 receipt/finality remain authoritative.
- Acceptance proof: fabricated BOUND payload, stale binding/generation, tenant/account/provider/destination/handle drift, whitespace/raw-canonical collision, surplus/missing/wrong-version/schema parity, changed replay and containment/normal kill flips at initial, credential-hold and execute/disposition phases all deny. Focused/full/compliance/typecheck/lint/build and isolated apply/reverse/reapply pass.
- Irreversible effects: none. The v2 migration is unapplied outside a disposable database and its preflighted reversal refuses downgrade when v2 rows exist.
- Non-scope: populating or choosing a live canonical binding source, resolving credential material, deleting credentials, invoking providers, changing Core/effect/operations-ledger bytes, production migration/query, deploy/promotion, merge/default action, activation or rollback.

