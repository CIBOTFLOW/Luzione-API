# Release Manifests

Exact release manifests are added here only after an implementation candidate exists. A manifest records candidate SHA, contract/schema versions, deployment evidence, feature flags, verification, rollback and remaining uncertainty. Directory presence is not release evidence.
