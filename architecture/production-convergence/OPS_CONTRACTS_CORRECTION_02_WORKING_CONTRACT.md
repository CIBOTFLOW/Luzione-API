# OPS-CONTRACTS-CORRECTION-02 working contract

- Project/outcome: close the two decision-semantic failures in `OPS-CONTRACTS-ASSURE-02` by publishing additive source-bound authority and incident-recovery contracts without changing v1/v2 bytes or meanings.
- Entrypoint/end state: an offline consumer supplies a strict `LuzioneOperationsEvidenceLedger/v3` plus an explicit assessment clock and server-resolved source snapshot; the L1 SDK either returns a source-bound, zero-credit structural result or a typed fail-closed error.
- Truth/write owner/readback: Luzione API owns the parser and versioned contracts. Human membership, approval, incident and recovery truth must come from an independently resolved source snapshot. L2/L3 packets may be `ABSENT`; neither consumer may mint authority. No route or truth store is added.
- Consumes/publishes: consumes frozen `LuzioneOperationsEvidenceLedger/v2`, assurance fingerprint `b11f3cb5b9901a37a36c2543aba3542ef8e4b7349f62d14bac16e9813faa103f`, and source-map fingerprint `66ca2ccbaca70ef60b407be7ac9230184b55cdeccbd9c50fdc67566802d72959`; publishes `LuzioneOperationsEvidenceLedger/v3`, `HumanAuthoritySourceBinding/v1`, `G2EffectAuthorityGrant/v2`, `IncidentRecoverySourceBinding/v1`, and `CapabilityEpochReset/v2` with schema, SDK, fixtures and L2/L3 packets.
- Dependencies/mutation cone: exact base `ef4d69145192da627184c73ac170a623ab6dc72e`; additive operations-evidence v3 schema/manifest, SDK/types/rules/fixtures/tests and execution evidence only.
- Reuse/freeze: v3 invokes the frozen v2 parser for append/reference/calendar/completeness/hard-zero/30-day structural validation, then requires exact source-binding parity before exposing a v3 result. Core, onboarding, effect-admission, connector-revocation and all v1/v2 operations-evidence artifacts remain byte-identical.
- Invariants: exact field/version/digest parsing; server-resolved source equality; active, current, same-tenant human membership; canonical role/function; one append-only approval per exact action/stage/effect; resolved-verified acknowledged incident and incident-bound recovery; deterministic server-derived reset fields; continuous single-successor epoch lineage; no owner/string reseal or agent identity; zero proof-day/G2/production credit.
- Non-scope: v1/v2 decision-bearing use, source adapter activation, routes, relations, migrations, credentials, providers, deployment, production query/write, proof-window opening, G2 authorization, promotion, merge, default-branch action or rollback.
- Acceptance proof: exact positive source-bound fixture; all required identity, membership, grant, incident, recovery and epoch adverse probes; bidirectional schema/parser parity; focused/full compliance/typecheck/lint/test/build; frozen-tree and reverse-apply proof; exact-head CI if a draft PR is available; detached exact-final attestation.
- Irreversible effects: none. Removing the additive v3 paths and metadata restores the exact base tree.
