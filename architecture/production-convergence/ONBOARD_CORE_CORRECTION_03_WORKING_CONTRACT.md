# ONBOARD-CORE-CORRECTION-03 working contract

- Project: `ONBOARD-CORE-CORRECTION-03` under controller `CIBOTFLOW/Luzione-platform-program@e698099e8b4b2de8abf3a9373fa7521fab0556ff`.
- Capability outcome: close only the remaining executable reverse-safety and corrected-v2 consumer-pin defects from `ONBOARD-CORE-CORRECTION-ASSURE-02` (`0133e77b326c26c670a562442e07b12baa952e007a82ea1573cfba9df702a2d0`) and `ONBOARD-CONSUMER-L2-V2-PIN-MAP-01` (`228027003e10cb2cfa6a9140daebc47eecf687f86deecd21fbb458548c9537b7`).
- Entrypoints and end state: the existing default-off import POST/GET and sandbox connector-validation POST advertise and parse the same v2 import identity; malformed original identifier bytes deny before reservation. The disposable reverse harness executes seven isolated populated preflight refusals and one empty reverse/reapply.
- Authoritative truth and write owner: Luzione Core remains the sole issuer of Blueprint, Mandate, Import and Connector receipts/readback. The existing tenant-scoped append-only Postgres tables remain the only persistence truth. L2 bindings remain an empty deny-all configuration unless explicitly admitted by the API.
- Consumed contracts: the frozen `LuzioneCoreContracts/v1`, `LuzioneOnboardCoreApi/v2`, `TenantBlueprintMap/v2`, `TenantPackSourceBinding/v1`, `LuzioneHumanApprovalSubject/v1`, `SetupMandateRevocation/v1`, `CRMImportDryRunMap/v2`, `ConnectorSyncValidation/v2` and `ConnectorValidationOutcome/v1`.
- Published boundary: corrected route identity, strict original-byte import/connector parsing, six additional populated reverse fixtures, deterministic disposable proof and an exact L2 repin packet. No existing contract identifier changes.
- Dependencies: exact API base `c7a0b46dbc3b776f49fd8d151c221c9d023a85d8`; the completed Correction-02 migration/reverse; existing P110 reservation and append-only onboarding tables; SandboxEcho only.
- Mutation cone: onboarding import/connector parsers and tests, import route metadata, disposable reverse fixtures/harness, execution ledgers and handoff evidence. No new route, relation, migration, provider, credential path or scheduler.
- Reuse/preservation: preserve Core, effect-admission, connector-revocation, operations-v1/v2/v3 and all existing onboarding contract/migration bytes. `CRMImportDryRunMap/v1` remains compatibility-fixture-only and is prohibited at decision-bearing POST/GET boundaries.
- Invariants: actor and tenant remain server-derived; v2 and v1 identity cannot be mixed; source binding remains default-empty; every effect remains `NO_EFFECT`; provider acknowledgement is not source finality; all populated reverse refusals occur before DDL and preserve exact rows plus schema.
- Non-scope: support/provider-registration work, production database query or migration, real credentials/providers, hosted effect, deployment/promotion, activation, merge/default-branch action, destructive reverse or production rollback.
- Acceptance proof: focused onboarding/import/connector tests; original-byte 201-plus, Unicode, whitespace and raw/canonical-collision negatives; mixed v1/v2 rejection; empty reverse/reapply; seven isolated populated pre-DDL refusals with identical before/after schema+row snapshots; full tests, compliance, typecheck, zero-warning lint and build; exact base-to-implementation reverse-apply check; exact-head CI if available without production polling.
- Irreversible effects: none. Reverse/reapply is disposable, empty and pre-activation only; populated evidence must refuse.
