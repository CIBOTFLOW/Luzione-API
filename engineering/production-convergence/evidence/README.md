# Evidence Packages

Evidence packages are immutable, exact-SHA records for API-PC project boundaries. They must reference actual commands/tests/deployments/readbacks and preserve failures, deferred infrastructure and uncertainty. This directory starts empty because program setup is not itself execution proof.
