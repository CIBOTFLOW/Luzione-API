# OPS-CONTRACTS-CORRECTION-03 G0 evidence

The exact-base, documentation/schema/SDK/test-only candidate closes the five assurance defects through an additive tenant-scoped append state and signed canonical-source-byte envelope. It adds no route, relation, migration, credential resolver, provider call, deployment or runtime activation.

The strict parser stores stable G2 identity across invocations, treats exact replay as idempotent, and rejects any changed approval source, signer, action, stage, effect, expiry, state or digest. The tenant-scoped state prevents separate ledger documents from forking a capability epoch. Exact canonical typed object/readback bytes are hashed and verified with a pinned synthetic Ed25519 public key; the private fixture seed is not present. Synthetic and ABSENT sources confer no decision authority.

Calendar credit is rejected on the reset day and begins strictly after the latest verified reset. All D01-D06 and B07-B11 probes pass locally. Frozen v1/v2 and domain trees are unchanged.

Local evidence: focused 10/10, operations-evidence 46/46, full repository 444/444, compliance 7/7, typecheck, zero-warning lint and Next.js 16.3.4 webpack build all pass. Exact-head CI and preview readback were not observed at this local evidence seal. Proof-day, G2 and production credit remain zero; G1, G2, integration and production readiness are not claimed.

The exact base-to-implementation diff passed a reverse-apply check with exit 0. This is source-tree reversal proof only; it is not production rollback authority or managed recovery evidence.
